{% include "problem/snippets/summary.twig" %}

<h3 class="light-blue-text text-darken-3">Vizualizare test #{{ testID }}</h3>

{% if not canViewTest %}
	<div class="card purple darken-2">
		<div class="card-content white-text">
			<span class="card-title">Test blocat</span>
			<p>
				Acest test îți este momentan blocat. Dacă dorești să vezi fișierele prin
				intermediul cărora programul tău este testat, poți debloca acest test contra
				sumei de <strong>5 puncte</strong>.				
			</p>
		</div>
		<div class="card-action">
			<a href="?p=problem&id={{ problemID }}&action=viewtest&testid={{ testID }}&unlock">Plătește <strong>5 puncte</strong></a>
		</div>
	</div>
{% else %}
	<div class="card-panel green darken-2">
		<span class="white-text">
			Vizualizezi testul cu ID-ul <code>#{{ testID }}</code>, în valoare de {{ test['points'] }} puncte.
		</span>
	</div>
	<div class="row">
		<div class="col s12 m6">
			<div class="card blue-grey darken-1">
				<div class="card-content white-text">
					{% if problemInfo['IOMethod'] == 'file' %}
						<span class="card-title">Intrare ({{ problemInfo['cleanName'] }}.in)</span>
					{% else %}
						<span class="card-title">Intrare</span>
					{% endif %}
					<p><code>{{ inputFile|nl2br }}</code></p>
				</div>
			</div>
		</div>
		<div class="col s12 m6">
			<div class="card blue-grey darken-1">
				<div class="card-content white-text">
					{% if problemInfo['IOMethod'] == 'file' %}
						<span class="card-title">Ieșire ({{ problemInfo['cleanName'] }}.out)</span>
					{% else %}
						<span class="card-title">Ieșire</span>
					{% endif %}
					<p><code>{{ outputFile|nl2br }}</code></p>
				</div>
			</div>
		</div>
	</div>
{% endif %}